# Secrets / config
.env
.videorc
video.toml

# Media — never commit video/audio (huge; GitHub charges for LFS). Back these up elsewhere.
*.mp4
*.mov
*.mkv
*.webm
*.wav
*.m4a
*.mp3
raws/
work/
clips/
ready/

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS
.DS_Store

# Hive machine-local session/telemetry state (contains local paths, session IDs) —
# not project knowledge, not meant for other contributors. Planning artifacts under
# .pHive/epics/, .pHive/project-profile.yaml, .pHive/CONTEXT.md, etc. are still tracked.
.pHive/agent-complete/
.pHive/interrupts/
.pHive/metrics/
.pHive/session-effort.txt
